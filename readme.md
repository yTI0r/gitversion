Версионирование на основе git. Для случаев когда разработка ведётся в VS+CMAKE на удалённом хосте.

* Установка
```
git submodule add <git@github > dir
```
Так же не забудьте добавить в .gitignore
```
vesion.h
```